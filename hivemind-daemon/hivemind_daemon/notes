scenario 1, I have a file that I want translated

scenario 2, the daemon machine has a bunch of files I want translated
    explicitly unsupported, daemon is isolated from host state

scenario 3, I have a bunch of files I want translated
--> Should (can) this be done with one request? or many?

Scenario 4, I want to pipe something through networks A -> arbitrary code -> B -> arbitrary code -> out
--> Should this be one request or many?

I kinda expect 
    > sync request
to emit a blob of binary output, but I'd like to abstract the whole thing

So how about 
/network/run
looks like a _very_ thin wrapper around tfserving or whatever

GTK / webkit html + js + UI provides a binding that lets you load a python module and call it and get results